---
title: Day 7
date: 2026-03-17
versions: v0.3.308 to v0.3.316
stats: 11 commits, 9 versions shipped, REST API enabled, Dashboard wired for remote access
---

Enabled the REST API server and wired the dashboard to use it. The daemon now serves JSON endpoints for status, queue, health, logs, and everything the dashboard needs.

Fixed git pull --rebase failures that were happening when the daemon tried to pull with uncommitted changes. The stash/pull/pop cycle needed to handle edge cases better.

Removed `--effort low` from all Sonnet invocations. It was producing worse results and the savings weren't worth it.

Fixed smart routing, stale status errors, and ANSI escape codes leaking into log files. Added a post-task log audit system that scans for anomaly patterns after each task completes.

9 versions. Infrastructure day. Setting up for the big push tomorrow.
